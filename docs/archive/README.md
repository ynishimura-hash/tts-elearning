# Archive

## tts-app-diffs-20260418.patch

2026-04-18 に削除した `/Users/yuyu24/2ndBrain/tts-app` フォルダとの差分記録。

- `tts-app` は当リポジトリ（`TTSシステム/app`）の古いローカルコピーで、Git リモート未設定のまま残っていた
- ファイル構成は同一、実質的に内容が異なるのは 12 ファイルのみ
- 差分内容はすべて「`TTSシステム/app` 側で後から改修された箇所の旧版」
  - 例: 個別相談の決済ステータス切替、管理者日時直接入力、Q&A 動画モーダル 等
- 救うべき新規実装は無かったため、フォルダ自体はゴミ箱へ移動した
- 本ファイルは後日参照できるよう、念のため unified diff を保存したもの
